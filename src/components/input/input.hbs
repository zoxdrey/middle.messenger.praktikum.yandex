<div>
  <label class="form__label" for="{{ id }}">{{ label }}</label>
  <input class="form__input" placeholder="{{ placeholder }}" type="{{ type }}" name="{{ name }}"
         id="{{ id }}" value="{{ value }}"/></div>
{{#if isError}}<p class="input__error">{{error}}</p>{{/if}}

